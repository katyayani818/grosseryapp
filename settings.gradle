dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        def repository = jcenter()
        repository // Warning: this repository is going to shut down soon
    }
}
rootProject.name = "abdo"
include ':app'
